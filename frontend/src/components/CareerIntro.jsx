import React from "react";

export default function CareerIntro() {
  return (
    <div className="flex justify-center items-center h-screen w-full bg-transparent">
      {/* Content removed as requested — add your new layout or components here */}
    </div>
  );
}
